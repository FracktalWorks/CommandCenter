# Skill Workbench — Control Plane (Phase 0.5 placeholder)

This folder will host the **Next.js + CopilotKit + AG-UI** three-pane control plane (WBS 0.5.x). Empty until Phase 0.5 starts.

Planned panes (per `ai-company-brain/system_architecture.md` §11):
1. **Chat / Agent Inbox** — CopilotKit + AG-UI (MAF agents).
2. **Skill Studio** — Monaco + Promptfoo runner.
3. **Observability** — audit log + spend (OTLP backend TBD).

Scaffold with `pnpm create next-app@latest .` when Phase 0.5 kicks off.
