[project]
name = "agent-app-builder"
version = "0.1.0"
description = "App Workshop builder agent — builds Custom Apps from chat"
requires-python = ">=3.12"
dependencies = [
    "agent-framework-github-copilot",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
