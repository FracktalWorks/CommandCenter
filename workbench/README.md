# Skill Workbench — Control Plane (Phase 0.5 placeholder)

This folder will host the **Next.js + CopilotKit + AG-UI** four-pane control plane (WBS 0.5.x). Empty until Phase 0.5 starts.

Planned panes (per `ai-company-brain/system_architecture.md` §11):
1. **Chat / Agent Inbox** — CopilotKit + AG-UI + LangGraph Agent Inbox.
2. **Skill Studio** — Monaco + embedded OpenHands + Promptfoo runner.
3. **Observability** — embedded Langfuse.
4. **Workflow Editor** — embedded n8n.

Scaffold with `pnpm create next-app@latest .` when Phase 0.5 kicks off.
