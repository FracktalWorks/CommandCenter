/**
 * Projects · the inline group-context add (WS-27y) — now the SHARED component.
 *
 * Moved to `src/components/QuickAdd.tsx` when /tasks grew the same
 * group-context add: one capture box, two apps. This shim keeps every
 * existing Projects import working unchanged.
 */

export { QuickAdd } from "@/components/QuickAdd";
